
  <h3 class = "center">Wellcome <%= @user.username %></h3>
  <div class = "row">
    <div class = "co-md-4 center">
      <ul class = "listing">
        <li class = "article-title"><%= gravatar_for @user,150 %></li>
        <li><small><strong><%= @user.username %> have <%= pluralize(@user.articles.count, "article") if @user.articles%></strong></small></li>
      </ul>
    </div>
    
  </div>
  <div align="center">
    <%= will_paginate @user_articles %>
  </div>
  <%= render '/articles/articles' , obj: @user_articles %>
  <div align="center">
    <%= will_paginate @user_articles %>
  </div>